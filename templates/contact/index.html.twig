{% extends 'base.html.twig' %}

{% block title %}CONTACTEZ-NOUS{% endblock %}

{% block body %}

    {% for message in app.flashes('message') %}
            <div class="alert alert-success">{{ message }}</div>
        {% endfor %}

    <div class="row">
        <div class="col-md-4">
            <br><br> <br> 
            <strong>La Boutique Dabchi.Com</strong>
            <hr>
            +216 21 598 206<br/>
            8 rue de Madrid, Tunis.<br/>
            amrourabti@gmail.com<br/>
        </div>
        <div class="col-md-8">
            <br> <br>
            <h4 class="section_title">Nous contacter</h4>
            <br>
            <p> <b>N'hésitez pas à prendre contact avec notre équipe, nous vous répondrons dans les meilleurs délais.</b></p>
            <p>Par e-mail en remplissant le formulaire ci-dessous.</p>

            {{ form_start(form) }}
                <div class="form-group"> {{ form_row(form.firstName, { 'label': 'Nom','attr': {'class': 'contact_input'}}) }} </div>
                <div class="form-group"> {{ form_row(form.lastName, { 'label': 'Prénom', 'attr': {'class': 'contact_input'}}) }} </div>
                <div class="form-group"> {{ form_row(form.email, { 'label': 'Email','attr': {'class': 'contact_input'}}) }} </div>
                <div class="form-group"> {{ form_row(form.message, {'label': 'Message', 'attr': {'class': 'contact_input'} }) }} </div>
                <button class="btn btn-lg btn-info btn-block mt-3" type="submit">
                    Envoyer 
                </button>
            {{ form_end(form) }} <br> <br> <br>
        </div> 
    </div>

                                
{% endblock %}
 