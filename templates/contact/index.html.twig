{% extends 'base.html.twig' %}

{% block title %}CONTACTEZ-NOUS{% endblock %}

{% block body %}


    {% for message in app.flashes('message') %}
        <div class="alert alert-success">{{ message }}</div>
    {% endfor %}

    <h4 class="section_title">Contact Us</h4>
        {{ form_start(form) }}
            <div class="form-group"> {{ form_row(form.firstName, { 'label': 'Nom','attr': {'class': 'contact_input'}}) }} </div>
            <div class="form-group"> {{ form_row(form.lastName, { 'label': 'Prénom', 'attr': {'class': 'contact_input'}}) }} </div>
            <div class="form-group"> {{ form_row(form.email, { 'label': 'Email:','attr': {'class': 'contact_input'}}) }} </div>
            <div class="form-group"> {{ form_row(form.message, {'label': 'Message', 'attr': {'class': 'contact_input'} }) }} </div>
            <button class="btn btn-lg btn-primary" type="submit">
                Envoyer 
            </button>
        {{ form_end(form) }}
                                
{% endblock %}
 