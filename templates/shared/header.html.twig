
<header>
    <nav class="navbar navbar-expand-md navbar-dark fixed-top" style="background-color: #095A63;">
        <a class="navbar-brand" class="color:black;" href="{{ path('home')}}">Dabchi.COM</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    {{ knp_menu_render('main') }}
                </li>
                
            </ul>
            <div class="navbar-items-custom">
                {% if app.user %}
                    <i class="bi bi-person-circle"></i><a href="{{ path('account')}}"> Mon Compte <small>({{ app.user.firstname}} {{ app.user.lastname }})</small></a> | <i class="bi bi-box-arrow-right"></i><a href="{{ path('app_logout')}}"> Déconnecter</a>
                {% else %}
                    <i class="bi bi-person-fill"></i><a href="{{ path('app_login')}}"> Connexion</a> | <a href="{{ path('app_register')}}">Inscription</a> 
                {% endif %}
                
                <a href="{{ path('cart')}}"><img src="{{ asset('assets/img/panier.png')}}" alt="mon_panier" height="20px" style="margin-left: 20px;"> </a> 
        
            </div>
        </div>
    </nav> 
</header>